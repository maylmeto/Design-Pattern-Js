// index of function

const courses = ["javaScript", "html", "css", "bootsrap"];

const numbers = [2, 6, 7];

const courseName = courses.map(tempVar => tempVar.length);

const dividedNum = numbers.map(tempVar => tempVar / 2);

console.log(dividedNum);
